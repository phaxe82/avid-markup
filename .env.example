# Copy this file to `.env` (gitignored) and fill in your token.
# HF_TOKEN is a one-time download key for the gated pyannote diarization model.
# Without it, transcription still works but every line is one unlabelled speaker.
HF_TOKEN=
